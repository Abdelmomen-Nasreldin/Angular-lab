import { IsPurshasedDirective } from './is-purshased.directive';

describe('IsPurshasedDirective', () => {
  it('should create an instance', () => {
    const directive = new IsPurshasedDirective();
    expect(directive).toBeTruthy();
  });
});
